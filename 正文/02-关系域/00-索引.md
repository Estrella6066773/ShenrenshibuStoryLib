# 关系域索引

关系域回答“对象之间如何连接”。

## 关系类型

- 控制（control）
- 依赖（dependency）
- 冲突（conflict）
- 交易（transaction）
- 渗透（infiltration）

## 录入规则

- 关系词条最少包含 `source` 与 `target`。
- 一条关系只描述一种主关系，复合关系拆分为多条。
- 关系强度、时效性、证据来源必须显式标注。
