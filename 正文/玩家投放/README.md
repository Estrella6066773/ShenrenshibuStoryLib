# 对玩家冻结层（玩家投放）

本目录收录**已通过审批、可向指定范围玩家或公众披露**之文案与导览。其内容应为 `设定真值/` 之审慎子集，并按**产品版本**分目录冻结。

## 版本目录规则

- 每一子版本（如 `v0.1`）须包含：`README.md`、`CHANGELOG.md`、`manifest.yaml` 与 `对外读本/`。  
- `manifest.yaml` 列明本版本对外有效之设定 `id` 集合；增删须伴随 `CHANGELOG` 记录。  
- 未经审批不得将 `visibility: internal` 之条文整段复制至 `对外读本/`。
